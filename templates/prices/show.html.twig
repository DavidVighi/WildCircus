{% extends 'base.html.twig' %}

{% block title %}Prices{% endblock %}

{% block body %}
    <h1>Prices</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ price.id }}</td>
            </tr>
            <tr>
                <th>Days</th>
                <td>{{ price.days }}</td>
            </tr>
            <tr>
                <th>Adultprices</th>
                <td>{{ price.adultprices }}</td>
            </tr>
            <tr>
                <th>Kidsprice</th>
                <td>{{ price.kidsprice }}</td>
            </tr>
            <tr>
                <th>Groups</th>
                <td>{{ price.groups }}</td>
            </tr>
            <tr>
                <th>School</th>
                <td>{{ price.school }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prices_index') }}">back to list</a>

    <a href="{{ path('prices_edit', {'id': price.id}) }}">edit</a>

    {{ include('prices/_delete_form.html.twig') }}
{% endblock %}
